{
  "permissions": {
    "deny": [
      "Edit(/convex/**)",
      "Write(/convex/**)",
      "Read(.env.local)",
      "Edit(.env.local)",
      "Edit(.env*)",
      "Bash(git push --force*)",
      "Bash(git reset --hard*)",
      "Bash(git clean*)",
      "Bash(rm -rf *)"
    ],
    "ask": [
      "Bash(git commit*)",
      "Bash(git push*)"
    ]
  }
}
