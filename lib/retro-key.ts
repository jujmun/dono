/**
 * Retro keyboard-key pressable: flat at rest, hard shadow + lift on hover,
 * snaps flat while pressed. Stays flat after click (data-retro-flat / :focus)
 * so it doesn't spring back to the hover lift on release.
 */
export const retroKeyClass = "retro-key";

/** Same motion with mint shadow (owned / accent surfaces). */
export const retroKeyMintClass = "retro-key-mint";
